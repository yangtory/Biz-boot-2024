spring:
  application.name: BookProject

  thymeleaf:
    prefix: classpath:/views/
    suffix: .html
    mode: HTML

  mvc.static-path-pattern: classpath:/static/**
  web.resources.static-locations: classpath:/static/

  config:
    import: classpath:db_config.yaml

logging:
  level.com.callor.book: debug